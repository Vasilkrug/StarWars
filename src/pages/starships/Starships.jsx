import React from 'react';
import './Staeships.scss';
import {CardList} from "../../components";

const Starships = () => {
    return (
        <div>
            <CardList title={'starships'}/>
        </div>
    );
};

export default Starships;